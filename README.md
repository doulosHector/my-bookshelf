# Mi Libero Personal

A personal reading log: track the books you read, rate them, write short reviews
and look at your reading stats. Data lives in `localStorage`, so nothing leaves
your device. Installable as a PWA and it works offline.

Built with React 19 + Vite.

## Getting started

```bash
npm install
npm run dev
```

| Script            | What it does                                          |
| ----------------- | ----------------------------------------------------- |
| `npm run dev`     | Dev server at http://localhost:5173                   |
| `npm run build`   | Production build into `dist/`                         |
| `npm run preview` | Serve the production build locally                    |
| `npm run lint`    | Run oxlint                                            |
| `npm run icons`   | Regenerate the PWA icons in `public/`                 |

## Project structure

```
src/
├── components/
│   ├── ui/          Reusable primitives (Badge, Stars, Modal, BarChart…)
│   ├── layout/      Header and tabs
│   ├── shelf/       Book list, cards and status filter
│   ├── stats/       Reading statistics tab
│   └── book-form/   Create/edit dialog and the Open Library search
├── hooks/           useBooks, useTheme, useLocalStorage, useOpenLibrarySearch
├── services/        localStorage wrapper and the Open Library client
├── utils/           Pure helpers (stats, spine colors, book normalization)
├── constants/       Design tokens, statuses, genres, storage keys
├── context/         Theme context and provider
└── styles/          Shared inline styles built from the theme tokens
```

## Deploying to GitHub Pages

1. Create a repository on GitHub and push this project to the `main` branch:

   ```bash
   git remote add origin https://github.com/<user>/<repo>.git
   git push -u origin main
   ```

2. In the repository, go to **Settings → Pages** and set **Source** to
   **GitHub Actions**.

3. Every push to `main` runs `.github/workflows/deploy.yml`, which builds the
   app and publishes it to
   `https://<user>.github.io/<repo>/`.

The workflow passes the repository name as `BASE_PATH`, so the asset paths are
correct whatever the repository is called. For a custom domain (or any host that
serves the app from the root), build with `BASE_PATH=/ npm run build`.

## PWA

The service worker is generated by `vite-plugin-pwa` with `registerType:
"autoUpdate"`: a new version is picked up on the next visit. The app shell,
Google Fonts and the last Open Library responses are cached, so the bookshelf
stays usable without a connection.

Service workers only run over HTTPS (or `localhost`), so the offline behaviour
shows up on the deployed site and in `npm run preview`, not in `npm run dev`.
